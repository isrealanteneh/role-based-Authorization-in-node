{
  "properties": {
    "user_id": {
      "type": "integer",
      "description": "ID of the user."
    },
    "user_name": {
      "type": "string",
      "description": "The name of the user."
    },
    "passwords": {
      "type": "string",
      "description": "The password of the user."
    },
    "roles": {
      "type": "string",
      "description": "The role of the user."
    }
  },
  "required": ["user_name", "passwords", "roles"]
}
